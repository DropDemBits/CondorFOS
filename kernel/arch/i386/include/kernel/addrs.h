/**
 * addrs.h: Address types for memory locations
 */
#include <condor.h>

#ifndef ADRRS_H
#define ADRRS_H

/**
 * Physical address (32 bits)
 */
typedef udword_t physical_addr_t;

/**
 * Linear (virtual) address (32 bits)
 */
typedef udword_t linear_addr_t;

#endif
